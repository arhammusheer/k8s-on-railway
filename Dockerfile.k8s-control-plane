# Use a base image (e.g., Ubuntu)
FROM ubuntu:latest

# Install kubeadm, kubelet, and kubectl
RUN apt-get update && apt-get install -y kubeadm kubelet kubectl

# Initialize the control plane (replace with actual commands)
CMD ["kubeadm", "init"]
